define(function(require) {
	require('project');

    var page = {
        init: function(){
            this.events();
        },
        events: function(){
            
        }
    }
    page.init();
});