define(function(require) {
	require('project');
	require('headerTab');

    var page = {
        init: function(){
            this.events();
        },
        events: function(){
            
        }
    }
    page.init();
});